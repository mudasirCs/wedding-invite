# Static site for Cloudflare Workers / Workers Builds
name = "wedding-invite"
compatibility_date = "2026-09-12"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"

# Custom domain (zone must be on Cloudflare first — see setup steps)
[[routes]]
pattern = "rafiwedsabeda.online"
custom_domain = true

[[routes]]
pattern = "www.rafiwedsabeda.online"
custom_domain = true
