# Cloudflare Pages — use these settings in the dashboard:
#   Framework preset: Vite
#   Build command:    npm run build
#   Build output:     dist
#   Root directory:   /
#   Deploy command:   (leave empty — do NOT use wrangler deploy)
#
# Environment variable (Production + Preview):
#   VITE_RSVP_SHEET_URL = your Google Apps Script /exec URL
